{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "defuse folder",
            "type": "coreclr",
            "request": "launch",
            "program": "${workspaceFolder}/bin/Debug/net9.0/defuse.exe",
            "args": ["merge", "--input", "${workspaceFolder}/defuse.Tests/Assets/"],
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "console": "internalConsole"
        }
    ]
}
